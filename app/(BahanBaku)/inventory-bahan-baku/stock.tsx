import { Cores } from "@/components/core";

const Stock = () => {
  return (
    <Cores.Modal
      title="Stock"
      onClose={() => {}}
      maxHeight="[96%]"
      maxWidth="[96vw]"
      className="mx-20"
    >
      <h1>Stock</h1>
    </Cores.Modal>
  );
};

export default Stock;
